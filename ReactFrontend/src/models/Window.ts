export type Window = {
  windowId: string;
  sk: string;
  windowDuration: string;
  pk: string;
};

export const defaultWindow: Window = {
  windowId: "",
  sk: "",
  windowDuration: "",
  pk: "",
};
